\section*{Methods}
Our goal is to find a lower bound for estimation of a connectivity
parameter between regions. To this end we formulate a discrete SIR
model and employ the Cramer-Rao lower bound to the connectivity
parameter between two regions.

\subsection*{The Cram\'er-Rao Lower Bound}

The \emph{Cram\'er-Rao Lower Bound} (CRLB) provides a fundamental limit
on estimation precision. For any unbiased estimator $\hat{\phi}$ of a
scalar parameter $\phi$, the CRLB states:
\begin{equation}\label{eq:crlb_statement}
\text{Var}(\hat{\phi}) \geq \frac{1}{J(\phi)}
\end{equation}
where $J(\phi)$ is the \emph{Fisher Information}. The Fisher
information measures how sharply the (log-)likelihood changes with the
parameter $\phi$. When the Fisher information is large, small changes
in $\phi$ produce large changes in the likelihood, so $\phi$ can be
estimated precisely. When the Fisher information is low, the
likelihood is relatively flat in $\phi$, so many nearby values of
$\phi$ explain the data almost equally well, making estimation
intrinsically noisy. So high Fisher info means the likelihood contains
a “narrow peak” around the true parameter; low Fisher information
implies the likelihood has a ``broad, flat peak''.

For a vector of parameters $\boldsymbol{\phi} = (\phi_1, \ldots,
\phi_p)$, the bound generalizes to:
\begin{equation}\label{eq:crlb_vector}
\text{Var}(\hat{\phi}_k) \geq [\mathbf{J}^{-1}]_{kk}
\end{equation}
where $\mathbf{J}$ is the $p \times p$ \emph{Fisher Information
Matrix} with entries
$$
J_{kl} = \mathbb{E}\left[\frac{\partial \ell}{\partial \phi_k}
  \frac{\partial \ell}{\partial \phi_l}\right].
$$
%
It is best to think of $\mathbf{J}$ as statistical precision, so
$\mathbf{J}^{-1}$ is just the a covariance matrix and (e.g.) its
$(1,1)$ entry is the variance of the estimator of $\phi_1$.

The CRLB has a key implication: if $[\mathbf{J}^{-1}]_{kk}$ is large,
parameter $\phi_k$ cannot be reliably estimated \emph{regardless of the
estimation method employed}. No amount of algorithmic sophistication can
overcome a fundamental lack of information in the data.



%% \input{contacts}
\subsection*{Discrete-Time Cross-Immunity Model}

The connectivity model assumes that all contacts lead to infection in
the same compartment. An alternative formulation applies to
cross-immunity between strains or pathogens, where exposure to a
related strain can confer immunity without causing infection by that
strain~\citep{gog2002}. We consider uni-directional cross-immunity:
exposure to strain $j$ confers immunity to strain $i$, but not vice
versa. The continuous-time equations are:
\begin{align*}
  \dot{S} &= -\beta(t) S \circ (I_d + M) I \\
  \dot{I} &= \beta(t) S\circ I - \nu I\\
  \dot{R} &= \beta(t) S \circ MI + \nu I,
\end{align*}
where the unidirectional cross-immunization matrix is
\begin{equation*}
M =
\begin{bmatrix}
  0 & \theta \\
  0 & 0
\end{bmatrix}
\end{equation*}
%% Anallogously, the force of infection in the discrete connectivity
%% model, we define the force of infection as 
%% \begin{equation*}
%% \lambda(t) = \beta(t) I(t)
%% \end{equation*}
The expected incidence is:
\begin{equation}\label{eq:cross_incidence}
\mu(t) = S(t) \left[1 - \exp(-\beta I) \right]
\end{equation}
The rest of the discrete-time dynamics arise naturally when we
carefully balance the terms to have a constant population:
\begin{align}\begin{split}\label{eq:discrete_cross}
S(t+1) &= S(t) \exp(-\beta (I_d + M)I) \\
I(t+1) &= I(t) \exp(-\gamma) + \mu(t) \\
R(t+1) &= R(t) + I(t) [1 - \exp(-\gamma)] + S(t)\exp(-\beta I(t))[1-\exp(-\beta MI(t))]
\end{split}\end{align}

\subsection*{Sensitivity equations}
The incidence sensitivity is:
\begin{align}\begin{split}\label{eq:sens_mu_cross}
    \frac{\partial \mu}{\partial \phi} %&= \frac{\partial}{\partial \phi}S[1-\exp(-\beta I) \\
    %
    %
    %
    &= \frac{\partial S}{\partial \phi} \left[1 - \exp(-\beta I)\right] +
    S \exp(-\beta I) \frac{\partial \beta I}{\partial \phi} \\
    %
    &=\frac{\partial S}{\partial \phi} \left[1 - \exp(-\beta I)\right] +
    \beta S \exp(-\beta I) \frac{\partial I}{\partial \phi}
\end{split}\end{align}
%% where
%% \begin{equation*}
%% \frac{\partial \lambda}{\partial \phi} = \beta
%% \frac{\partial I}{\partial \phi}
%% \end{equation*}
%
%
%% Note that $\theta$ does not appear in $\lambda_i$ or $\mu_i$
%% directly.  The parameter $\theta$ affects future incidence through
%% the depletion of susceptibles.
The state sensitivities propagate via:
\begin{align}\begin{split}\label{eq:sens_cross}
\frac{\partial S(t+1)}{\partial \phi} &=
\frac{\partial S}{\partial \phi} \exp(-\beta(I_d +M) I) - \beta S \exp(-\beta(I_d + M)I) \circ \left(
\frac{\partial I}{\partial \phi} + M\frac{\partial I}{\partial \phi} + \frac{\partial M}{\partial \phi}I \right) \\
\frac{\partial I(t+1)}{\partial \phi} &= \exp(-\gamma)
\frac{\partial I}{\partial \phi} + \frac{\partial \mu}{\partial \phi}
\end{split}\end{align}
%% nd
%% \begin{equation*}
%% \frac{\partial MI}{\partial \phi} = \beta \left(
%% \theta \frac{\partial I_j}{\partial \phi} + \delta_{\phi,\theta} I_j \right)
%% \end{equation*}
%% with $\delta_{\phi,\theta} = 1$ if $\phi = \theta$ and 0 otherwise.
%% The CRLB derivation proceeds as before, with
%% $\mathbf{J} = \mathbf{G}^T \mathbf{W} \mathbf{G}$.




\subsection*{Optimization}

To validate the CRLB predictions, we perform parameter inference on
synthetic data generated by simulating the model with known parameters
and adding binomial observation noise (Eq.~\ref{eq:obs_binomial}).

We minimize the negative log-likelihood (Eq.~\ref{eq:loglik}) subject
to $\theta \in [0, 0.5]$, $S_i(0), I_i(0) \in [0,1]$, and $S_i(0) +
I_i(0) \leq 1$. The gradient is minus the score function
(Eq.~\ref{eq:score}), with $\partial\mu_i(t)/\partial\phi$ from the
sensitivity equations (Eq.~\ref{eq:sens_mu}). This gradient is computed
exactly in a single forward pass alongside the state and sensitivity
equations.

We use the \texttt{NLopt} library with the SLSQP algorithm for
gradient-based optimization. To handle potential local minima, we
employ a multi-start strategy with $N_0 = 20$ random initial points,
selecting the solution with lowest objective value. Optimization runs
use relative tolerance $10^{-4}$ and are parallelized across CPU
cores.

\subsection*{Real Data}

To validate our theoretical predictions on empirical data, we analyze
pneumonia and influenza (P\&I) mortality in the United States. We
integrate two publicly available data sources: weekly P\&I death
counts from the CDC FluView surveillance system
\cite{cdcfluview2024} and the HealthData.gov P\&I mortality
dataset \cite{healthdatagov2024}. Where these sources overlap
temporally and geographically, we average the reported death counts,
as the two datasets show strong agreement (see Results). Population
denominators are obtained from the CDC WONDER database
\cite{cdcwonder2024} and interpolated linearly to weekly resolution.

The observation model for mortality data uses an infection fatality
rate (IFR) as the reporting rate $\rho$. Following
\citet{wong2013case}, we estimate the IFR for seasonal influenza as
approximately $\rho \approx 5.4 \times 10^{-4}$ (0.054\%), computed
from the ratio of reported P\&I deaths to estimated total infections.

The seasonal phase $\varphi_i$ for each region is estimated from
absolute humidity data \cite{shaman2010absolute}. We find the phase
offset that maximizes the correlation between $\sin(2\pi t +
\varphi)$ and negative absolute humidity, aligning the model's
seasonal forcing with humidity-driven transmission dynamics.

For both CRLB computation and parameter inference, we exclude
observations with incidence below $10^{-6}$ per capita. Such low
values provide negligible Fisher information and can numerically
destabilize the bound calculation through division by near-zero
incidence in the weight matrix.
